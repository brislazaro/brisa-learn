/*
    1- Crea una calculadora que pida al usuario (con prompts) dos valores y un operador (+, -, *, /).
    Tiene que calcular el resultado y mostrarselo al usuario con el formato:

    2 + 2 = 4
*/

/*
    2- Ahora, valida que los valores que introduzca el usuario sean numeros SIN FUNCIONES.
        - Si el usuario cancela el prompt, es decir nos llega null. Lo interpretaremos como un 0.
        - Si el usuario intenta introducir un valor que no es un numero, 
          le mostraremos un error, y le volveremos a pedir el numero.
*/

/*
    3- Haz lo mismo para validar el operador. Si el usuario introduce un operador que no es valido, 
    le mostraremos un mensaje de error y lo volveremos a pedir hasta que sea valido.
*/

/*
    4- Ahora mueve ese codigo de la validacion de un numero a una funcion aparte y llamala cuando la necesites.
*/

/*
    5- Haz lo mismo, pero con la validacion del operador.
*/

/*
    6- Ahora crea 4 funciones, una para cada operacion basica (suma, resta, multiplicacion y division).
    Y llamalas donde sea correspondiente.
*/

/*
    7- Por ultimo, crea una funcion que se llame formatResult, que reciba un numero.

    Si este numero tiene decimales, debera devolverlo solo con 2 decimales.

    Sino, debera devolverlo sin decimales.
*/
