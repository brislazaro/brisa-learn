const producto3 = "monitor de 20 pulgadas";
const precio = "30 USD";

console.log(producto3.concat(precio));
console.log(producto3 + precio);
console.log(`el productor ${producto3} tiene un precio de ${precio}`);
