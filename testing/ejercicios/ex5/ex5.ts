/*
    Aqui tienes una funcion que recibe un array de numeros, y retorna un nuevo array con los numeros multiplicados por 2.

    Testea esta funcion. Asegurate de hacer varios escenarios.
*/

function multiplyByTwo(array: number[]): number[] {
  return array.map((num) => num * 2);
}
